{
  "mcpServers": {
    "filesystem": {
      "command": "npx",
      "args": [
        "-y",
        "@modelcontextprotocol/server-filesystem",
        "${ALLOWED_DIRECTORY}"
      ],
      "env": {
        "ALLOWED_DIRECTORY": "${ALLOWED_DIRECTORY}"
      }
    }
  }
}
